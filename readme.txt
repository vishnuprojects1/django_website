If you want to the see the website content in your browser:
1. Create a folder and move all these files to it.
2. Inside that folder run command prompt and type "python manage.py runserver".
3. Copy the link obtained into the browser.